<%= content_for :head do %>
<script type="text/javascript">
    $(function() {
    });
</script>
<% end %>

<%= content_for :header do %>
<h1>Listing client_transactions</h1>
<% end %>

<%= content_for :main_content do %>
<table>
  <tr>

    <th>Client</th>

    <th>Player</th>

    <th>Investment type</th>

    <th>Investment round price</th>

    <th>Amount invested</th>

    <th>Date transacted</th>

    <th>Expected date of return</th>

    <th></th>
  </tr>

<% @client_transactions.each do |client_transaction| %>
  <tr>

    <td><%= client_transaction.client_id %></td>

    <td><%= client_transaction.player_id %></td>

    <td><%= client_transaction.investment_type_id %></td>

    <td><%= client_transaction.investment_round_price_id %></td>

    <td><%= client_transaction.amount_invested %></td>

    <td><%= client_transaction.date_transacted %></td>

    <td><%= client_transaction.expected_date_of_return %></td>

    <td>
    <%= link_to image_tag(icon("show"),:size=>"64x64"), client_transaction, :title=>'Show' %>
    <%= link_to image_tag(icon("edit"),:size=>"64x64"), edit_client_transaction_path(client_transaction), :title=>'Edit' %>
    <%= link_to image_tag(icon("delete"),:size=>"64x64"), client_transaction, confirm: 'Are you sure?', method: :delete, :title=>'Delete' %>
    </td>
  </tr>
<% end %>
</table>
<% end %>

<br />
<%= content_for :navigation do %>
<% end %>
<%= content_for :extra do %>
<%= link_to image_tag('new_icon.png',:size=>"100x100"), new_client_transaction_path, :title=>"Add a client_transaction" %>
<% end %>
<%= content_for :footer do %>
<% end %>

